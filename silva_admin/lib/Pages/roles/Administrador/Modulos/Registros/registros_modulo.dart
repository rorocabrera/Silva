import 'package:flutter/material.dart';

class RegistrosOptPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Modulo de Registros')),
        body: Center(child: Text("Pagina de Registros")));
  }
}
