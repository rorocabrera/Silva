import 'package:flutter/material.dart';

class AdminRegEqpPage extends StatelessWidget {
  const AdminRegEqpPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Text("Pagina de Equipos"),
    ));
  }
}
