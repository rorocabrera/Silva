import 'package:flutter/material.dart';

class BioSignUp extends StatelessWidget {
  const BioSignUp(this.index);
  final int index;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Only for Mobile"),
      ),
    );
  }
}
